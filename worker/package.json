{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute norheimsposten-db --local --file=src/db/migrations/0001_initial.sql",
    "db:migrate:remote": "wrangler d1 execute norheimsposten-db --file=src/db/migrations/0001_initial.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
